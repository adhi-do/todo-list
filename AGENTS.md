# Project Agent

## GitHub Repository
- **Remote URL:** `git@github.com:adhi-do/todo-list.git`

## Tech Stack
- HTML5
- CSS3 (Vanilla)
- JavaScript (Vanilla)

## App Launch Commands
Since this is a static web application, there are no build steps required. You can launch it using a simple static file server.

**Using Python:**
```bash
python3 -m http.server 8000
```
Then navigate to `http://localhost:8000` in your browser.

**Using Node.js:**
```bash
npx serve .
```

Alternatively, you can simply open the `index.html` file directly in any modern web browser.
